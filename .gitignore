node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
.DS_Store

# Packaged build artefact — regenerated on demand, never versioned
*.zip

# Local verification screenshots
.shots/

# Media-generation intermediates (reproducible)
.media-tmp/

# Raw client-supplied source media — processed into public/media/* by
# scripts/build-desktop-assets.mjs; not itself needed by the running site
cineheight-desktop-assets/
